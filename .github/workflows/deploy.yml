name: Deploy Git2Page

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
    - name: Run tests
      run: cargo test --verbose

  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Log in to the Container registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha,prefix={{branch}}-
          type=raw,value=latest,enable={{is_default_branch}}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
    - name: Validate deploy secrets
      env:
        DEPLOY_HOST: ${{ secrets.SSH_HOST || secrets.HOST || secrets.SERVER_HOST }}
        DEPLOY_USER: ${{ secrets.SSH_USER || secrets.USERNAME }}
        DEPLOY_KEY: ${{ secrets.DEPLOY_SSH_KEY || secrets.SSH_KEY }}
      run: |
        if [ -z "$DEPLOY_HOST" ]; then
          echo "Missing deploy host secret. Set one of: SSH_HOST, HOST, SERVER_HOST"
          exit 1
        fi
        if [ -z "$DEPLOY_USER" ]; then
          echo "Missing deploy username secret. Set one of: SSH_USER, USERNAME"
          exit 1
        fi
        if [ -z "$DEPLOY_KEY" ]; then
          echo "Missing deploy key secret. Set one of: DEPLOY_SSH_KEY, SSH_KEY"
          exit 1
        fi

    - name: Deploy to production
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.SSH_HOST || secrets.HOST || secrets.SERVER_HOST }}
        username: ${{ secrets.SSH_USER || secrets.USERNAME }}
        key: ${{ secrets.DEPLOY_SSH_KEY || secrets.SSH_KEY }}
        script: |
          cd /opt/git2page
          docker-compose -f docker-compose.yml -f docker-compose.prod.yml pull
          docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
          docker system prune -f
